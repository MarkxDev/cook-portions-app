import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.cookportions.app',
  appName: 'Cook Portions',
  webDir: 'dist/cook-portions-app/browser'
};

export default config;
